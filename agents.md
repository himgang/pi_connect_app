# Agent Workflow

- Never push directly to the `main` branch.
- For every change, create a new branch, commit there, push that branch, and create a pull request.
- Keep commits scoped to the requested work and avoid staging unrelated local changes.
